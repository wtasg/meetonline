/**
 * Cookie configuration utilities
 * Provides consistent cookie options across the application
 */

/**
 * Default cookie options for clearCookie operations
 * @type {Object}
 */
const COOKIE_CLEAR_OPTIONS = {
    domain: "localhost",
    path: "/"
};

export { COOKIE_CLEAR_OPTIONS };
